# requirements.txt
# File này chứa danh sách các thư viện Python cần thiết để chạy ứng dụng.
# Cài đặt tất cả bằng câu lệnh: pip install -r requirements.txt

selenium>=4.0.0
beautifulsoup4>=4.10.0
PyQt6>=6.0.0
webdriver-manager>=3.5.0
pandas>=1.3.0
